import { Example } from "./Example";

import styles from "./Example.module.css";

export type ExampleModel = {
    text: string;
    value: string;
};

const EXAMPLES: ExampleModel[] = [
    {
        text: "What are the monitoring and supervision policy covers?",
        value: "What are the monitoring and supervision policy covers?"
    },
    { text: "What is an FTA?", value: "What is an FTA?" },
    { text: "What are the responsibilities and obligation for crypto currency policy?", value: "What are the responsibilities and obligation for crypto currency policy?" }
];

interface Props {
    onExampleClicked: (value: string) => void;
}

export const ExampleList = ({ onExampleClicked }: Props) => {
    return (
        <ul className={styles.examplesNavList}>
            {EXAMPLES.map((x, i) => (
                <li key={i}>
                    <Example text={x.text} value={x.value} onClick={onExampleClicked} />
                </li>
            ))}
        </ul>
    );
};
